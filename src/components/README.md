# Components

All UI components for SecureVault Dashboard will go here.
Per the project requirements, **no external component libraries** (MUI, Chakra, etc.) can be used. Every component must be hand-built using the design tokens in `src/styles/theme.css`.

Expected:

- `FileTree/`
- `PropertiesPanel/`
- etc.
